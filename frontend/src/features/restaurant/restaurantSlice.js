import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const restauranrtSlice = createSlice({
  name: "restaurant",
  initialState,
  reducers: {},
});

export const {} = restauranrtSlice.actions;
export default restauranrtSlice.reducer;
